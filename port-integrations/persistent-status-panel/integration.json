{
  "id": "persistent-status-panel",
  "title": "Persistent Status Panel",
  "description": "Keep the ChatGPT /status panel open across thread switches and app restarts until explicitly closed.",
  "defaultEnabled": true,
  "entrypoints": {
    "patchDescriptors": "./patch.js"
  }
}
