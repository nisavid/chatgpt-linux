# 2026-07-31 Sync Ledger

## Scope

- PR: this sync PR
- Origin base before sync:
  `98f2338d19fb148867d9fe64062694a3b870db0d`
- Pre-merge task-branch commit:
  `2bfd4b78c6811c42b9bb655ae7505742e58dd6f9`
- Previous divergence baseline:
  `933413a679df51886ea05d1d0eeb8057e44bf764`
- Synced Linux-port upstream commit:
  `efe491761d9075341fe79f564631a6dd9aafd291`
- Incoming upstream commits: 817
- Upstream commits after the frozen ref: 0 at fetch time
- Required history shape: a normal merge commit with the pre-merge task branch
  as first parent and the frozen Linux-port upstream commit as second parent

## Policy And Decision Sources

- Read before conflict resolution:
  `AGENTS.md`, `docs/maintainers/fork-divergences.md`,
  `.agents/fork-sync-policy.toml`,
  `docs/maintainers/fork-sync-policy.md`,
  `docs/maintainers/package-runtime-maintenance.md`, and `CONTEXT.md`.
- Used the user-global `syncing-forks-with-upstream` skill.
- GitHub issues #119 through #126 record the completed Wayfinder map and its
  accepted identity, migration, capability, delivery, and verification
  contracts.
- The merge commit imports the frozen upstream behavior under the then-current
  `codex-app` and `codex-app-updater` contracts. Subsequent commits migrate
  port-owned identity to `chatgpt` and `chatgpt-updater` while preserving the
  merge's upstream ancestry.

## Incoming Catalog

- ChatGPT-era official app artifact and bundle support.
- Expanded Linux Computer Use runtime and browser integration.
- New and revised configurable port integrations.
- Patch-engine and official-DMG validation refactors.
- Updater CLI repair, restart, diagnostics, and package workflow changes.
- Nix, AppImage, native package, CI, issue-governance, and maintainer tooling
  updates.

## Local Reconciliation

- Preserve the former fork identity during the ancestry-preserving merge; apply
  the approved canonical ChatGPT migration in subsequent commits.
- Preserve the XDG/FHS native package layout, bundle-derived package version,
  unprivileged updater boundary, package verification, rollback hardening, and
  supply-chain gates.
- Reconcile upstream `linux-features/` changes into `port-integrations/`.
- Reconcile `updater/src/upstream.rs` into `updater/src/dmg_source.rs`.
- Reconcile upstream package, service, desktop, policy, and user-local files
  into the paths that existed at the merge checkpoint, then move them to the
  canonical `chatgpt` and `chatgpt-updater` paths.
- Reconcile upstream official-DMG workflow changes into
  `.github/workflows/official-dmg-build-app.yml`.
- Keep the current docs information architecture; adapt relevant upstream facts
  into the README and usage or maintainer references.

### Renamed-Path Reconciliation

- Ported `.github/workflows/upstream-build-app.yml` changes to
  `.github/workflows/official-dmg-build-app.yml`; the local release gate now
  accepts the current official DMG, while hosted checks remain a publication
  gate.
- Ported `updater/src/upstream.rs` changes to `updater/src/dmg_source.rs`;
  updater tests passed.
- Ported `packaging/appimage/codex-desktop.desktop` changes to
  `packaging/appimage/chatgpt.desktop`; the cross-format matrix exercised the
  renamed AppImage payload, and the final current-DMG package gate used the
  host-native pacman format.
- Ported native package, updater-helper, and user-local installer changes to
  the former local paths at the merge checkpoint, then moved the reconciled
  behavior to the canonical `chatgpt` and `chatgpt-updater` paths.
- Ported `linux-features/` manifests, resources, hooks, fixtures, and helper
  behavior to `port-integrations/`; the old root and helper files remain
  absent.
- Ported the functional `post_upgrade` service-lifecycle change from
  `packaging/linux/codex-desktop.install` to
  `packaging/linux/chatgpt.install`: fresh installs ensure the updater service,
  while upgrades only start a service the user already enabled.
- Ported disabled-integration cleanup-hook execution from
  `scripts/lib/linux-features.sh` to `scripts/lib/port-integrations.sh`; cleanup
  runs before staging and remains fail-closed.
- Ported official-DMG policy tokens, environment variables, targets, and
  validation-profile changes under the current local names; the final local
  source, generated-app, and pacman package gates passed.

## Capability Disposition

- Keep the established default integrations: Agent Workspaces, AppShots,
  ChatGPT wrapper updater, conversation mode, Copilot reasoning effort, Open
  Target Discovery, Read Aloud, Read Aloud MCP, remote-control UI, and
  remote-mobile control.
- Enable the accepted reviewed defaults: API-key model visibility, API-key
  service tier, global dictation, Omarchy theme, persistent status panel, Pet
  Overlay, project group and task sorting, shared app-server socket, SSH command
  wrapper, and UI Tweaks.
- Keep authenticated proxy, Codex Micro, directory-only and shallow repository
  watches, MCP helper reaping, and Record & Replay disabled by default because
  they remain deferred or carry material runtime/resource tradeoffs.
- Remove `zed-opener`; Open Target Discovery supersedes it.
- Treat `notification-actions-linux` as core compatibility.
- Limit Dock-icon writes and cleanup to marker-owned ChatGPT desktop and icon
  files; never mutate unmanaged launchers, icons, or favorites.
- Require all three Suggested Prompts conditions: official-app eligibility, the
  user's setting, and a supported local Linux patch contract.
- Apply Linux Computer Use support by default while preserving the official
  persistent feature and allowed-app controls plus Codex tool policy as the
  authorization boundary. Remove the fork-owned UI opt-in and add no duplicate
  consent prompt or persisted grant. Local support and host readiness remain
  execution preconditions, not permissions.

## Docs Review

- Upstream README and user-facing documentation review: complete under the
  canonical ChatGPT for Linux identity and current package/runtime contracts.
- Adapted under local contracts: README, CHANGELOG, CONTRIBUTING, retained usage
  and maintainer references, and applicable newly imported documents.
- Already covered: package, updater, launcher, and Computer Use facts already
  represented in the retained documentation structure.
- Intentionally omitted: the superseded architecture, build-and-packaging,
  Linux Computer Use, port integration architecture, native setup, Nix,
  troubleshooting, and updater documents.
- Follow-up: after this implementation PR is merged and verified, rename the
  GitHub repository in place to `nisavid/chatgpt-linux`, then verify remotes,
  redirects, workflows, release links, issues, and pull-request continuity.

## Divergence Review

- 1. Local product and package identity: migrated after the merge checkpoint
  under the accepted ChatGPT migration contract.
- 2. Linux filesystem layout and package payload contract: preserve the
  XDG/FHS shape while migrating port-owned names.
- 3. Package versioning from the official app bundle: preserve.
- 4. Package builder hardening: preserve and extend to canonical names.
- 5. Updater privilege boundary and install hardening: preserve.
- 6. Updater state, config overlay, and failure recovery: preserve and extend
  with journaled forward and reverse identity migration.
- 7. Codex CLI discovery and preflight: preserve OpenAI-owned Codex interfaces.
- 8. Generated launcher and packaged runtime behavior: preserve, then migrate
  port-owned names.
- 9. ASAR, port integration, and Linux UI patch behavior: reconcile against the
  new upstream patch engine and accepted capability disposition.
- 10. Webview server lifecycle: preserve under the canonical app identity.
- 11. Linux Computer Use integration compatibility: preserve and update.
- 12. Release, security, and supply-chain verification: preserve and update for
  `ChatGPT.dmg` and `ChatGPT.app`.
- 13. User-local install experiment identity and layout: migrate names while
  preserving XDG paths.
- 14. Maintainer policy, docs, and agent workflow: updated for the canonical
  identity; the in-place GitHub repository rename remains the final post-merge
  operation.

## Special Handling And Follow-Up

- The sync began with 148 unmerged paths. The index now has no unmerged
  entries, and the renamed-path audit has no open functional reconciliation.
- The incoming-hunk omission audit restored packaged-runtime session-variable
  import boundaries, legacy update-manager cleanup, and opt-in updater service
  startup. No other incoming functional omissions remain.
- No unresolved product decision remains from the completed Wayfinder map.
- Resolved the sensitive-desktop authorization question from issue #128:
  persistent grants belong to each feature's existing control plane, revocation
  blocks future invocations, and transport or readiness never authorizes an
  action.
- Reusable policy gaps closed during implementation: updated official-DMG
  terminology, advanced the divergence baseline to the frozen upstream ref,
  added regression coverage for package service lifecycle and
  disabled-integration cleanup, and restored the incoming webview server
  parent-death guard after the smoke gate exposed the omitted hunk.
- Classified all remaining legacy identity and environment references. OpenAI-
  owned Codex interfaces retain `CODEX_*`; port-owned variables use
  `CHATGPT_*`, and obsolete port-owned names fail with their exact replacement.
- Chromium rewrites its command line and clears its environment after startup.
  Warm-start PID validation therefore falls back to the exact local
  `--app-id=chatgpt` argument only after matching the Electron path and current
  user. The regression fixture reproduces both rewritten `/proc` surfaces.
- Codex CLI discovery preserves the visible `codex` invocation identity for
  native multicall executables while pinning the canonical executable target.
  This prevents a `codex -> vp` installation from being re-invoked as `vp` and
  aborting app-server bootstrap; the launcher policy fixture compiles and
  exercises the same basename-sensitive native shape.
- The final DMG refresh advanced the official app from `26.803.41515` to
  `26.803.61601`. The project-group last-updated sorter was adapted to the
  current `D8o`/`F6i` contract, the superseded matcher was removed, and focused
  first-pass, second-pass, parse, and full-generation gates passed.
- Extended the rename map from `codex-app` to `chatgpt` and from
  `codex-app-updater` to `chatgpt-updater`, including package transitions,
  XDG migration, port-owned environment variables, integration ids, generated
  metadata roots, and the final repository slug.

## Verification

### History And Source

- `git fetch --prune origin` and `git fetch --prune upstream`: passed.
- Merge commit `33e81d2346ca1f4ccbb4acc307d78c52e712c304` has
  pre-merge task commit `2bfd4b78c6811c42b9bb655ae7505742e58dd6f9` as
  first parent and frozen Linux-port upstream
  `efe491761d9075341fe79f564631a6dd9aafd291` as second parent.
- Linux-port upstream had no commits after the frozen ref at merge time.
- Unmerged-path, renamed-path, incoming-hunk omission, diff-check, shell syntax,
  Python syntax, and JSON parsing audits: passed. The final change set contains
  405 paths; 397 exist at the final source shape.
- Final `bash scripts/ci/run-node-checks.sh`: 1,546 tests, 1,545 passed and one
  intentional skip. The real Codex authority and UID-sensitive private-staging
  fixtures were run in the normal host ownership context.
- Final scrubbed-host `bash tests/scripts_smoke.sh`: passed, including package
  formats, transactional mutation failure, both launcher recovery modes,
  rewritten-process warm handoff, and all 24 webview probe scenarios. The
  scrubbed environment excludes retired port-owned variables exported by the
  old installed app while tests supply inherited OpenAI `CODEX_*` interfaces
  explicitly.
- Rust formatting, workspace checks, tests, and Clippy: passed; 884 tests passed
  across 29 harnesses, including 388 updater unit tests and five updater
  integration tests.
- Final identity sweep: no suspicious legacy local identity or environment
  reference. Remaining `codex-app`, `codex-desktop`, old repository slug,
  and former port-owned `CODEX_*` occurrences are inherited OpenAI
  interfaces, Linux-port upstream references, or explicit migration,
  compatibility, policy, test, and historical evidence.

### Official Artifact And Generated App

- The cached official DMG was refreshed on 2026-08-10 before the final build.
  SHA-256:
  `f02641573285e88312f07fabe694cfbb01859de244e25c6ce57ed526facac56b`;
  size 556,141,982 bytes. The artifact reports `Last-Modified: Mon, 10 Aug
  2026 18:21:25 GMT`.
- Final generation used a scrubbed build environment and `./install.sh` with
  `CHATGPT_INSTALL_DIR` pointed at the side-by-side `chatgpt-next-app` target so
  validation did not interrupt the live remote-control instance.
- Generated version: app `26.803.61601`, bundle `6396`, Electron `42.3.0`.
- Transaction `20260810T200459-2712960-13088`: accepted and atomically promoted
  to the side-by-side target;
  blockers, warnings, and inconclusive reasons were empty.
- Patch report: 149 entries, with 128 applied and 21 already applied. Required
  core contracts accounted for 18 applied and five already applied entries;
  the required private bundled-plugin staging integration also applied. All 21
  accepted default port integrations were enabled.
- Generated identity is `chatgpt`, `ChatGPT`, and `chatgpt.desktop`; package
  version remains sourced from `CFBundleShortVersionString`.
- Post-patch integrity inspection reported zero findings. Current official
  main and webview drift repairs were independently exercised for first-pass
  application, second-pass byte stability, and parseability before the final
  build.
- Earlier Xvfb and KDE/X11 cold and warm starts passed with the real Codex CLI,
  including rewritten-process warm handoff and clean shutdown. A final
  post-merge runtime and installed-package pass remains a publication gate.

### Packages And Migration

- The final host-native package is
  `chatgpt-26.803.61601-1-x86_64.pkg.tar.zst`, SHA-256
  `dae4afd36f212978993dbfefeb9c2f8974d954d56460eb41eaa62cd2b20be945`.
  Its metadata reports `chatgpt` version `26.803.61601-1`, the canonical
  repository URL and description, and the required `codex-app` and
  `codex-desktop` provide/conflict/replace transitions. The full first package
  listing was inspected.
- The earlier cross-format matrix used app version `26.727.40816`. Debian on
  Ubuntu 24.04 produced
  `chatgpt_26.727.40816_amd64.deb`, SHA-256
  `50a271d2bce834d3f4a23d125e70b5a2019f35658a91b1fb0674490258dcf8c3`.
- RPM on the current CachyOS host:
  `chatgpt-26.727.40816-1.x86_64.rpm`, SHA-256
  `f0b95d5d8bc28b6fbf3be9e22547fae9eeda6447a300b42ce079974b0e6c7436`.
- Pacman on the current CachyOS host:
  `chatgpt-26.727.40816-1-x86_64.pkg.tar.zst`, SHA-256
  `3928c0c224dc36ceb32c1aad58f18a0e0d64511a46a215d82a45343dd665f19b`.
- AppImage:
  `chatgpt-26.727.40816-x86_64.AppImage`, SHA-256
  `8475bf9c8aa4b722ab8ed451cc2a6789dde3ceaf4eca82773cff004627265b66`.
- All matrix package metadata and full first listings were inspected. Native
  packages provide, conflict with, and replace `codex-app` and `codex-desktop`,
  use canonical paths, and ship no legacy launcher or service shim. AppImage
  keeps manual updates and excludes native updater payloads.
- Debian disposable lifecycle: passed legacy-package replacement, no mutation
  at package-install time, collision refusal, implicit forward migration,
  idempotence, explicit reverse migration, a second forward migration,
  uninstall, package-payload cleanup, and preservation of user-owned data.
- Arch pacman disposable lifecycle: passed the same replacement, migration,
  collision, ownership, uninstall, payload-cleanup, and user-data-preservation
  contract. Forward migration used a bounded ordinary launcher invocation;
  `chatgpt migrate-state --reverse` remained the explicit reversal interface.
- Fedora 42 RPM disposable lifecycle: passed the same replacement,
  migration, collision, ownership, uninstall, payload-cleanup, and user-data-
  preservation contract. Its bounded ordinary launcher invocations completed
  migration before the expected later failure in the dependency-minimal
  `--nodeps` container.

### Remaining Publication Gates

- Codex Security diff scan: resume the preserved scan from the generated
  ChatGPT build and seal its canonical artifacts.
- CodeQL, Clippy SARIF, Nix, openSUSE dependency bootstrap, Debian CI, Apple DMG
  verification, independent repository review, CodeRabbit fixed point, PR
  checks, merge ancestry, and the post-merge in-place repository rename remain
  publication gates.
